spring:
  profiles:
    active: "local"
  main:
    banner-mode: "off"
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: false
    show-sql: true
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/latihan_spring_baru?serverTimezone=Asia/Jakarta
    username: postgres
    password: admin12345
server:
  port: 9090
  servlet:
    context-path: /api
  #logging:
  # level:
  #  root: INFO